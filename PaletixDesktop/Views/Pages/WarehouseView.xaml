<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="PaletixDesktop.Views.Pages.WarehouseView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="28,22,28,28" RowSpacing="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Spacing="4">
            <TextBlock FontSize="30" FontWeight="SemiBold" Foreground="{StaticResource AppPrimaryTextBrush}" Text="Magatzem" />
            <TextBlock Foreground="{StaticResource AppSecondaryTextBrush}" Text="{Binding Subtitle}" />
        </StackPanel>
        <Border Grid.Row="1" Padding="22" Style="{StaticResource ShellCardBorderStyle}">
            <StackPanel Spacing="10">
                <TextBlock FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource AppPrimaryTextBrush}" Text="{Binding Title}" />
                <TextBlock Foreground="{StaticResource AppSecondaryTextBrush}" Text="Selecciona Stock, Ubicacions o Lots al panell lateral per obrir la vista funcional corresponent." TextWrapping="Wrap" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
